media:
  input: public/images
  output: /images
  categories: [image]
  rename: safe

content:
  - name: news
    label: Vijesti
    type: collection
    path: src/content/news
    format: yaml-frontmatter
    filename: "{fields.title}.mdoc"
    view:
      fields: [title, date, summary]
      primary: title
      sort: [date, title]
      search: [title, summary, body]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Naslov
        type: string
        required: true
        description: Naslov vijesti koji se prikazuje na websiteu.
      - name: date
        label: Datum objave
        type: date
        required: true
        options:
          format: yyyy-MM-dd
      - name: summary
        label: Kratki sažetak
        type: text
        description: Kratki opis koji se prikazuje na popisu vijesti.
        options:
          maxlength: 300
      - name: coverImage
        label: Naslovna slika
        type: image
        description: Opcionalna slika koja se prikazuje uz vijest.
        options:
          path: public/images/news
          categories: [image]
          extensions: [jpg, jpeg, png, webp, gif]
          rename: safe
      - name: body
        label: Sadržaj
        type: rich-text
        required: true
        options:
          format: markdown
          switcher: true
          path: public/images/news
          categories: [image]
          extensions: [jpg, jpeg, png, webp, gif]
          rename: safe

  - name: events
    label: Događaji (Upcoming Events)
    type: collection
    path: src/content/events
    format: yaml-frontmatter
    filename: "{fields.title}.md"
    view:
      fields: [title, date, location]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: asc
    fields:
      - name: title
        label: Naslov događaja
        type: string
        required: true
        description: Naziv događaja koji se prikazuje na naslovnici.
      - name: date
        label: Datum
        type: date
        options:
          format: yyyy-MM-dd
        description: Datum događaja. Ostavi prazno ako je "uskoro / detalji slijede".
      - name: dateLabel
        label: Tekst umjesto datuma (opcionalno)
        type: string
        description: Npr. "Uskoro — detalji slijede". Ako je ispunjeno, prikazuje se umjesto datuma.
      - name: time
        label: Vrijeme (opcionalno)
        type: string
        description: Npr. "13:30–14:30".
      - name: location
        label: Lokacija (opcionalno)
        type: string
        description: Npr. "Taipei, Taiwan".
      - name: body
        label: Detalji
        type: rich-text
        options:
          format: markdown
