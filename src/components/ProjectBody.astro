---
// Renders a CMS-managed service project's rich-text body inside a .prose wrapper.
// All content (text, images, tables, galleries) lives in the entry body.
import { render } from 'astro:content';
const { project } = Astro.props;
const { Content } = await render(project);
---
<Content />
